# YOLO11n vs YOLO26n 비교 (2026-08-03)

## 배경

기존 배포 모델(YOLO11n, `runs/train_3`)의 Recall이 낮아(0.537) 개선 여지가 있어 보여, 같은 연산량대의 차세대 아키텍처인 YOLO26n으로 비교 실험을 진행함.

- 두 모델 모두 **동일한 학습 조건**: 데이터(`data/train`, 옛 분할 기준 15,210장), epochs=50, imgsz=640, batch=16, seed=42
- YOLO26n은 21/50 epoch에서 중단 (성능 개선 추세가 이미 뚜렷해 조기 종료 판단, patience 조기종료 조건에는 도달하지 않음)

## 동일 epoch(21) 기준 비교 — 신뢰 가능한 결과

같은 학습 데이터, 같은 epoch 수에서 두 아키텍처를 비교한 값. 이 프로젝트에서 가장 신뢰할 수 있는 비교 지표.

| 지표 | YOLO11n (epoch21) | YOLO26n (epoch21) | 차이 |
|---|---|---|---|
| Precision | 0.542 | 0.523 | -3.5% |
| Recall | 0.458 | **0.519** | **+13.4%** |
| mAP50 | 0.495 | **0.517** | +4.5% |
| mAP50-95 | 0.242 | **0.258** | +6.6% |

**해석**: YOLO26n이 mAP50, Recall, mAP50-95에서 우세. 특히 Recall이 크게 개선됐는데(+13.4%), 불량 검사 태스크에서는 미검출(False Negative) 방지가 더 중요하므로 실질적으로 의미 있는 개선.

## 학습 속도 비교

| | YOLO11n | YOLO26n |
|---|---|---|
| epoch당 평균 시간 | 약 146초 | 약 231초 (1.6배 느림) |
| 아키텍처 | 182 layers, CNN | 260 layers, NMS-free, C2PSA attention |
| 파라미터 | 2.62M | 2.57M (거의 동일) |
| GFLOPs | 6.6 | 6.1 (오히려 약간 적음) |

같은 연산량(GFLOPs)인데도 실제 wall-clock 학습 시간은 YOLO26n이 더 오래 걸림. 레이어 수가 많고 구조가 복잡해(NMS-free head 등) 이 GPU(RTX 5060 Laptop)에서는 오버헤드가 있는 것으로 보임. 정확도는 epoch당 더 빨리 오르지만, 시간당으로 보면 이득이 상쇄되는 부분이 있음.

## 참고: YOLO11n vs YOLO11n 최종(50 epoch) 비교

| 지표 | YOLO11n epoch21 | YOLO11n 최종(epoch50) |
|---|---|---|
| mAP50 | 0.495 | 0.588 |
| Recall | 0.458 | 0.537 |

YOLO11n도 21 epoch 이후 계속 개선되었으므로, YOLO26n을 50 epoch까지 완주했다면 추가 개선 여지가 있었을 것으로 추정됨(다만 이번엔 21 epoch에서 중단하기로 결정).

## ⚠️ 중요: test_v2(재분할 데이터셋) 검증은 무효

`resplit_dataset.py`로 만든 `test_v2`(1,575장, family 단위로 유출 방지 처리된 재분할 데이터셋)로 두 모델을 재검증했으나, **결과가 오염되어 신뢰할 수 없음**:

| 지표 | YOLO11n (test_v2) | YOLO26n (test_v2) |
|---|---|---|
| mAP50 | 0.892 | 0.816 |
| Precision | 0.880 | 0.782 |
| Recall | 0.851 | 0.770 |

**원인**: 두 모델 모두 **옛 분할 기준의 `data/train`**으로 학습됨. `test_v2`는 옛 train+valid+test 전체를 재섞어 만든 것이므로, `test_v2`에 포함된 이미지 상당수가 두 모델의 학습 데이터에 이미 포함되어 있었음(데이터 유출). YOLO11n이 더 오래 학습해서(50 epoch) 더 많이 암기했기 때문에 YOLO26n보다 높게 나온 것으로 추정 — 이 자체가 유출을 뒷받침하는 정황.

**결론**: `train_v2`/`valid_v2`/`test_v2`로 두 모델을 처음부터 다시 학습해야만 공정한 재검증이 가능함. 현재는 미실시.

## v2 재학습 — 유출 없는 최종 검증 (2026-08-03 추가)

`train_v2`(12,843장)로 YOLO26n을 처음부터 50 epoch 재학습(`train.py`, `data/data_v2.yaml`, run: `train_20260803_1918`), `test_v2`(1,575장, family 단위로 train_v2와 완전히 분리됨)로 최종 검증.

**재현성 검증 완료**: 재학습 전 아래 항목 모두 확인함
- train_v2/valid_v2/test_v2 이미지·라벨 개수 정합성 (합계 15,969장, 원본과 일치)
- split 간 파일명 중복 0건
- family(원본+증강본) 단위 유출 0건 (14,769개 family 전수 확인)
- 학습 로그에서 `data/data_v2.yaml` 및 `valid_v2` 사용 확인, 에러/손상 파일 없음

### valid_v2 기준 학습 곡선 (50 epoch)

| epoch | mAP50 | mAP50-95 | Precision | Recall |
|---|---|---|---|---|
| 5 | 0.542 | 0.375 | 0.594 | 0.508 |
| 10 | 0.705 | 0.514 | 0.693 | 0.638 |
| 15 | 0.781 | 0.578 | 0.750 | 0.734 |
| 20 | 0.816 | 0.634 | 0.793 | 0.772 |
| 25 | 0.841 | 0.666 | 0.830 | 0.776 |
| 30 | 0.851 | 0.683 | 0.841 | 0.782 |
| 40 | 0.880 | 0.728 | 0.834 | 0.838 |
| 50(최종) | 0.881 | 0.744 | 0.874 | 0.831 |

### test_v2 최종 결과 (신뢰 가능, 유출 없음)

| 지표 | 값 |
|---|---|
| mAP50 | **0.830** |
| mAP50-95 | 0.707 |
| Precision | 0.835 |
| Recall | 0.796 |

**해석**: 옛 YOLO11n(valid셋 기준 mAP50 0.588)과 비교하면 크게 높지만, 이는 "모델이 더 좋아졌다"보다 **① test_v2가 유출 없는 더 정확한 평가라는 점, ② 이번엔 50 epoch를 다 채운 완전 학습이라는 점**이 함께 작용한 결과로 봐야 함. 진짜 공정 비교를 하려면 YOLO11n도 `train_v2`로 재학습해서 같은 `test_v2`로 검증해야 하나, 시간 관계상 이번엔 YOLO26n만 진행함.

### 알려진 이슈: train.py 후처리 단계 경로 버그

학습(50epoch)과 최종 검증 자체는 정상 완료됐으나, `train.py`의 학습곡선 저장 단계에서 에러 발생:
```
FileNotFoundError: [Errno 2] No such file or directory: 'runs/train_20260803_1918/results.csv'
```
실제 저장 경로는 `runs/detect/runs/train_20260803_1918/results.csv`. ultralytics 버전 변화로 `project`/`name` 인자를 줘도 `detect/` 하위에 재중첩되는 구조 변경 때문으로 보임(이전 `data.yaml` 경로 버그와 같은 계열의 버전 이슈). `train.py`가 `f"{PROJECT}/{RUN_NAME}/results.csv"`로 경로를 수동 조합하는 대신, `model.train()`의 반환값(`results.save_dir`)을 사용하도록 고치면 해결됨. 모델 가중치 저장에는 영향 없음 — 다음 정리 작업 때 수정 예정.

## 결론 및 다음 단계

1. 동일 조건(같은 옛 분할, 같은 epoch21) 비교에서도 YOLO26n이 우세 — mAP50 +4.5%, Recall +13.4%
2. v2로 50 epoch 완전 재학습한 결과, 유출 없는 test_v2 기준 mAP50 0.830 확보 — 준수한 수준
3. YOLO11n의 v2 기준 수치는 아직 없음 (미실시) — 필요 시 추후 동일 절차로 진행
4. 배포 모델 교체를 고려한다면 이번 YOLO26n(v2 학습본, `runs/detect/runs/train_20260803_1918/weights/best.pt`)이 유력한 후보
5. `train.py`의 결과 경로 버그 수정 필요 (다음 작업)
